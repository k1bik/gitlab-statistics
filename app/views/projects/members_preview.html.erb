<%= turbo_frame_tag :members_preview do %>
  <% members.each do |member| %>
    <%= tag.img(
      class: "members-preview-avatar",
      src: member.avatar_url,
      alt: member.name,
      title: member.name
    ) %>
  <% end %>
<% end %>
